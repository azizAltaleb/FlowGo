services:
  app:
    image: ${GOFLOW_IMAGE_REGISTRY:-goflow}/workflow-command:${GOFLOW_IMAGE_TAG:-0.1.0}
    build: !reset null

  workflow-runtime:
    image: ${GOFLOW_IMAGE_REGISTRY:-goflow}/workflow-runtime:${GOFLOW_IMAGE_TAG:-0.1.0}
    build: !reset null

  workflow-query:
    image: ${GOFLOW_IMAGE_REGISTRY:-goflow}/workflow-query:${GOFLOW_IMAGE_TAG:-0.1.0}
    build: !reset null

  sync-worker:
    image: ${GOFLOW_IMAGE_REGISTRY:-goflow}/sync-worker:${GOFLOW_IMAGE_TAG:-0.1.0}
    build: !reset null

  frontend:
    image: ${GOFLOW_IMAGE_REGISTRY:-goflow}/frontend:${GOFLOW_IMAGE_TAG:-0.1.0}
    build: !reset null

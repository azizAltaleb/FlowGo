apiVersion: v1
kind: Service
metadata:
  name: {{ include "artificialflow.commandServiceName" . }}
  labels:
    {{- include "artificialflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: command
spec:
  type: {{ .Values.command.service.type }}
  ports:
    - name: http
      port: {{ .Values.command.service.port }}
      targetPort: http
  selector:
    {{- include "artificialflow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: command
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "artificialflow.fullname" . }}-command
  labels:
    {{- include "artificialflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: command
spec:
  replicas: {{ .Values.replicaCount.command }}
  selector:
    matchLabels:
      {{- include "artificialflow.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: command
  template:
    metadata:
      labels:
        {{- include "artificialflow.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: command
      {{- with .Values.global.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "artificialflow.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: workflow-command
          image: "{{ .Values.images.command.repository }}:{{ .Values.images.command.tag }}"
          imagePullPolicy: {{ .Values.images.command.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: {{ include "artificialflow.envConfigName" . }}
          env:
            - name: PG_DSN
              valueFrom:
                secretKeyRef:
                  name: {{ include "artificialflow.envSecretName" . }}
                  key: {{ .Values.postgresql.existingSecretKey | default "PG_DSN" }}
            - name: AUTH_INTROSPECTION_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "artificialflow.envSecretName" . }}
                  key: AUTH_INTROSPECTION_CLIENT_SECRET
                  optional: true
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.command.resources | nindent 12 }}
          {{- if .Values.zitadel.enabled }}
          volumeMounts:
            - name: zitadel-bootstrap
              mountPath: /zitadel/bootstrap
              readOnly: true
            - name: artificialflow-bootstrap
              mountPath: {{ include "artificialflow.applicationBootstrapPath" . }}
              readOnly: true
            - name: artificialflow-auth
              mountPath: {{ include "artificialflow.applicationAuthPath" . }}
              readOnly: true
          {{- end }}
      {{- if .Values.zitadel.enabled }}
      volumes:
        - name: zitadel-bootstrap
          persistentVolumeClaim:
            claimName: {{ include "artificialflow.zitadelBootstrapPvc" . }}
        - name: artificialflow-bootstrap
          persistentVolumeClaim:
            claimName: {{ include "artificialflow.bootstrapPvc" . }}
        - name: artificialflow-auth
          persistentVolumeClaim:
            claimName: {{ include "artificialflow.authPvc" . }}
      {{- end }}
      {{- with .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

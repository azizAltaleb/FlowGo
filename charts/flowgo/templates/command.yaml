apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowgo.commandServiceName" . }}
  labels:
    {{- include "flowgo.labels" . | nindent 4 }}
    app.kubernetes.io/component: command
spec:
  type: {{ .Values.command.service.type }}
  ports:
    - name: http
      port: {{ .Values.command.service.port }}
      targetPort: http
  selector:
    {{- include "flowgo.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: command
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flowgo.fullname" . }}-command
  labels:
    {{- include "flowgo.labels" . | nindent 4 }}
    app.kubernetes.io/component: command
spec:
  replicas: {{ .Values.replicaCount.command }}
  selector:
    matchLabels:
      {{- include "flowgo.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: command
  template:
    metadata:
      labels:
        {{- include "flowgo.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: command
      {{- with .Values.global.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "flowgo.serviceAccountName" . }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: workflow-command
          image: "{{ .Values.images.command.repository }}:{{ .Values.images.command.tag }}"
          imagePullPolicy: {{ .Values.images.command.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: {{ include "flowgo.envConfigName" . }}
          env:
            - name: PG_DSN
              valueFrom:
                secretKeyRef:
                  name: {{ include "flowgo.envSecretName" . }}
                  key: {{ .Values.postgresql.existingSecretKey | default "PG_DSN" }}
            - name: AUTH_INTROSPECTION_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "flowgo.envSecretName" . }}
                  key: AUTH_INTROSPECTION_CLIENT_SECRET
                  optional: true
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.command.resources | nindent 12 }}
          {{- if .Values.zitadel.enabled }}
          volumeMounts:
            - name: zitadel-bootstrap
              mountPath: /zitadel/bootstrap
            - name: flowgo-bootstrap
              mountPath: /flowgo/bootstrap
              readOnly: true
          {{- end }}
      {{- if .Values.zitadel.enabled }}
      volumes:
        - name: zitadel-bootstrap
          persistentVolumeClaim:
            claimName: {{ include "flowgo.zitadelBootstrapPvc" . }}
        - name: flowgo-bootstrap
          persistentVolumeClaim:
            claimName: {{ include "flowgo.flowgoBootstrapPvc" . }}
      {{- end }}
      {{- with .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.global.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

services:
  app:
    image: ${FLOWGO_IMAGE_REGISTRY:-azizaltaleb}/workflow-command:${FLOWGO_IMAGE_TAG:-0.1.1}
    build: !reset null

  workflow-runtime:
    image: ${FLOWGO_IMAGE_REGISTRY:-azizaltaleb}/workflow-runtime:${FLOWGO_IMAGE_TAG:-0.1.1}
    build: !reset null

  workflow-query:
    image: ${FLOWGO_IMAGE_REGISTRY:-azizaltaleb}/workflow-query:${FLOWGO_IMAGE_TAG:-0.1.1}
    build: !reset null

  sync-worker:
    image: ${FLOWGO_IMAGE_REGISTRY:-azizaltaleb}/sync-worker:${FLOWGO_IMAGE_TAG:-0.1.1}
    build: !reset null

  frontend:
    image: ${FLOWGO_IMAGE_REGISTRY:-azizaltaleb}/frontend:${FLOWGO_IMAGE_TAG:-0.1.1}
    build: !reset null

GoFlow has been deployed.

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
IAM mode: {{ .Values.iam.mode }}

Application URL:
{{- if .Values.ingress.enabled }}
  https://{{ .Values.ingress.host }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "goflow.gatewayServiceName" . }} 8080:{{ .Values.gateway.service.port }}
{{- end }}

{{- if and .Values.zitadel.enabled (eq .Values.iam.mode "zitadel") }}
Bundled ZITADEL URL:
{{- if .Values.zitadel.ingress.enabled }}
  {{ .Values.zitadel.publicUrl }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "goflow.fullname" . }}-zitadel-api 8081:8080
{{- end }}

The frontend OIDC client ID is generated by the ZITADEL bootstrap process and written to /goflow/bootstrap/goflow-frontend-client-id.
{{- end }}

Health checks:
  kubectl -n {{ .Release.Namespace }} get pods -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl -n {{ .Release.Namespace }} get ingress

# Deprecated `@flowgo/nodejs-sdk` compatibility package

This package re-exports `@artificialflow/nodejs-sdk` for one transition release.
New and existing applications should install `@artificialflow/nodejs-sdk` and use
the `ArtificialFlowClient` and `ArtificialFlowApiError` exports.

# Thermo-Nuclear Review History

Durable audit ledger for FlowGo. This file is the source of truth for findings,
owner decisions, fixes, test notes, and manual QA status.

## Session Baseline

- Created: 2026-06-03
- Repository: `/Users/abdulazizaltaleb/Desktop/work/workflowsa`
- Product: FlowGo, an open-source BPMN workflow engine with Go backend, React/Vite frontend, CQRS projection, OIDC IAM, Docker/Helm deployment assets, and Node.js SDK.
- Branch: `main`
- HEAD: `575533db0a441ff53d08951647af56f81f3178f8`
- Base branch: `origin/main`
- Merge base: `575533db0a441ff53d08951647af56f81f3178f8`
- Initial working tree: clean before audit scaffolding; `.agents/` files added for this workflow.
- Package managers: Go modules; npm with `package-lock.json` in `frontend/`, `clients/nodejs-sdk/`, and `tests/e2e/playwright/`.

## Main App Folders

- `backend/`: Go services, shared libraries, generated protobufs, Dockerfiles.
- `frontend/`: React 19, Vite, TypeScript, Tailwind admin/modeler UI.
- `clients/nodejs-sdk/`: Node.js SDK package and generated gRPC client code.
- `tests/`: integration, performance, security, and Playwright e2e coverage.
- `scripts/`: release, smoke, CQRS, security, and report automation.
- `charts/flowgo/`: Helm production deployment chart.
- `docker-compose*.yml`: local/evaluation deployment profiles.
- `.github/workflows/`: CI, security, image release, npm release, release dry-run.

## Likely Commands

- Backend unit scope: `go test ./backend/... -count=1`
- BPMN matrix: `make test-bpmn-matrix`
- Focused outbox/idempotency regressions:
  `go test ./backend/services/workflow-command/internal/application -run 'Outbox|Idempotency' -count=1`
  `go test ./backend/services/workflow-command/internal/infrastructure/persistence -run 'Outbox|Idempotency|TerminalFailed' -count=1`
  `go test ./backend/services/workflow-command/internal/interfaces/http -run 'Idempotency|Protocol|Capabilities|Metrics' -count=1`
  `go test ./backend/services/sync-worker/... -count=1`
  `go test ./backend/libs/worker -count=1`
- Frontend install/lint/test/build:
  `npm --prefix frontend ci`
  `npm --prefix frontend run lint`
  `npm --prefix frontend test`
  `npm --prefix frontend run build`
- SDK install/test/package:
  `npm --prefix clients/nodejs-sdk ci`
  `npm --prefix clients/nodejs-sdk test`
  `npm --prefix clients/nodejs-sdk run validate:package`
  `(cd clients/nodejs-sdk && npm pack --dry-run)`
- Compose and release smoke:
  `make smoke-profiles`
  `make smoke-release-profiles`
  `make validate-helm`
  `make release-dry-run`
- Broader suites:
  `make test-unit`
  `make test-integration`
  `make test-e2e`
  `make test-frontend`
  `make test-security`
  `make test-all`

## Release-Critical Surfaces

- Startup and routing: backend service `cmd/server`, `cmd/runtime`, query API, sync worker, frontend `main.tsx` and `App.tsx`.
- Auth and permissions: OIDC/JWT/introspection libraries, role mapping, identity management handlers, frontend auth/runtime config.
- Workflow correctness: BPMN parser, workflow engine, navigation, events, jobs, gateway/subprocess/timer/message/error handling.
- Persistence and sync: command repository, outbox/idempotency, Kafka/NATS producers/consumers, Debezium/CQRS projection, Elasticsearch/OpenSearch repositories.
- API/data contracts: REST handlers, gRPC worker API, generated protobuf artifacts, Node SDK client.
- User-facing UI: dashboard, process/instance views, modeler, identity, SDK clients, settings-like account/admin surfaces.
- Release operations: Dockerfiles, compose profiles, Helm chart, release workflows, security workflow, dry-run scripts.
- Privacy/telemetry: analytics/event correctness if present; audit log and CDC data flow.

## Large / High-Coupling Hotspots

Generated or lock files are expected but still affect release packaging.

- `backend/services/workflow-command/internal/application/engine.go` - 965 LOC
- `backend/services/workflow-command/internal/domain/bpmn/parser.go` - 954 LOC
- `backend/services/workflow-command/internal/interfaces/http/handler.go` - 825 LOC
- `backend/services/workflow-command/internal/interfaces/http/handler_test.go` - 1725 LOC
- `backend/services/workflow-command/internal/domain/bpmn/parser_test.go` - 815 LOC
- `backend/libs/iam/zitadel_management_client.go` - 772 LOC
- `backend/services/workflow-command/internal/application/navigation.go` - 755 LOC
- `backend/services/workflow-command/internal/application/events.go` - 755 LOC
- `frontend/src/lib/api.ts` - 649 LOC
- `scripts/cqrs_e2e_smoke.sh` - 625 LOC
- `backend/services/workflow-command/internal/application/step_executors.go` - 610 LOC
- `frontend/src/lib/bpmn-parser.ts` - 593 LOC
- `backend/services/workflow-command/internal/infrastructure/persistence/gorm_repository.go` - 533 LOC
- `backend/services/sync-worker/internal/application/service.go` - 523 LOC
- `clients/nodejs-sdk/src/Client.ts` - 509 LOC
- `docker-compose.zitadel.yml` - 488 LOC
- `backend/services/workflow-command/cmd/server/main.go` - 457 LOC
- `backend/services/sync-worker/internal/infrastructure/messaging/kafka_consumer.go` - 457 LOC
- `frontend/src/pages/Modeler.tsx` - 340 LOC
- `frontend/src/components/bpmn/PropertiesPanel.tsx` - 319 LOC

## Audit Phases

| Phase | Scope | Status | Notes |
| --- | --- | --- | --- |
| 0 | App shell, startup, routing, providers, global error boundaries, config, env handling | Initial scan complete | Release boot subagent completed; parent-verified findings recorded below. |
| 1 | Auth, entitlements, permissions, subscriptions/payments, account state | Initial scan complete | Auth/API subagent completed; parent-verified findings recorded below. |
| 2 | Core product flows and main user-facing screens | Initial scan complete | Workflow UX subagent completed; parent-verified findings recorded below. |
| 3 | Data persistence, local storage, syncing, offline behavior, migrations | Initial scan complete | Persistence/CQRS subagent completed; parent-verified findings recorded below. |
| 4 | Backend/API/database/server functions, access control, validation, indexes | Initial scan complete | API access-control scan completed with Phase 1; lower-priority validation candidates remain unrecorded pending triage. |
| 5 | Analytics, telemetry, event correctness, privacy, attribution, funnels | Initial scan complete | Event/privacy scan completed with Phase 3; lower-priority candidates remain unrecorded pending triage. |
| 6 | Settings, account management, notifications, preferences | Not started | Identity/admin/client management surfaces. |
| 7 | Forms, validation, edge cases, empty/loading/error states | Initial scan complete | Covered through workflow UX audit; parent-verified findings recorded below. |
| 8 | Performance, large files, rendering, lists, expensive effects, bundle risks | Initial scan complete | Quality/tests subagent completed; parent-verified findings recorded below. |
| 9 | Platform-specific behavior, release/build config, crash handling | Initial scan complete | Release boot subagent completed; parent-verified findings recorded below. |
| 10 | Tests, QA coverage, regression checklist, release blockers | Initial scan complete | Quality/tests subagent completed; parent-verified findings recorded below. |

## Findings Ledger

| ID | Title | Severity | Status | Phase | File/Path | Evidence | Impact | Recommendation | Owner decision | Fix summary | Tests run | Manual QA notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P8-O1 | Signal publishing scans all active elements and re-parses BPMN per match attempt | Critical | Fixed | 8 | `backend/services/workflow-command/internal/application/events.go`; `backend/services/workflow-command/internal/domain/repository/repository.go`; `backend/services/workflow-command/internal/infrastructure/persistence/gorm_repository.go`; `backend/services/workflow-command/internal/application/engine_tx_test.go` | Parent re-triage 2026-06-15 plus read-only validation subagents: `PublishSignal` and internal `publishSignal` called `ListActiveElementInstances(ctx, 0)`; `processSignalForElement` loaded the instance and called `getWorkflowDefinition`; `processToWorkflowDefinition` called `bpmn.Parse` for XML process resources. | Signal API latency and CPU grew with total active element count; moderate production load could delay or fail signal correlations and stall workflows. | Add signal-name-indexed subscriptions or scoped queries, and cache parsed workflow definitions keyed by process/resource version. | Owner approved fixing on 2026-06-15 | Signal publish now uses a type-scoped active-element query for signal-capable elements, skips non-matching candidates before opening per-element transactions, memoizes workflow definitions per publish, and hydrates process instances only after a candidate step matches the signal. Regression tests cover scoped querying plus per-process definition memoization before instance hydration. | `go test ./backend/services/workflow-command/internal/application -run 'PublishSignal|Signal|WorkflowDefinition|CompleteJob' -count=1` passed after retrying with a workspace Go cache due toolchain download restrictions; `go test ./backend/services/workflow-command/internal/infrastructure/persistence -run 'Element|Signal|Process' -count=1`; `go test ./backend/services/workflow-command/internal/application -count=1`; `go test ./backend/services/workflow-command/... -count=1`; `git diff --check`; ReadLints clean | Unchecked; manually verify signal publish latency remains stable when many active non-signal elements exist, and matching BPMN signal catches still correlate in a live stack. |
| P8-O2 | BPMN deployment endpoint reads unbounded request body | High | Fixed | 8 | `backend/services/workflow-command/internal/interfaces/http/handler.go`; `backend/services/workflow-command/internal/interfaces/http/handler_test.go` | Parent spot-check: `deployWorkflow` called `io.ReadAll(r.Body)` directly; no `http.MaxBytesReader` or handler-level body limit was found. | Authenticated deploy clients can exhaust memory with oversized XML and degrade command service availability. | Enforce a maximum BPMN upload size and return 413 for oversized bodies before parsing. | Owner approved fixing with P8-Q1 on 2026-06-03 | `deployWorkflow` now wraps BPMN upload bodies with `http.MaxBytesReader` using a 20 MiB cap, returns 413 for oversized bodies, and has regression coverage that oversized deploys create no workflow. | `go test ./backend/services/workflow-command/internal/interfaces/http -run 'DeployBPMN' -count=1`; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1`; `git diff --check`; ReadLints clean | Unchecked; manually verify authenticated deploys under the limit still succeed and oversized deploys return 413 through direct command-service and gateway paths. |
| P8-Q1 | Full test orchestrator can report failures without failing the command | High | Fixed | 10 | `scripts/test-all.sh`; `Makefile` | Parent spot-check: `scripts/test-all.sh` recorded per-layer exit codes but reached the end without aggregating them into a non-zero exit; `make test-frontend` also swallowed Playwright failures with `|| true`. | Release and local QA can produce false-positive green results while important layers failed. | Aggregate test layer failures and exit non-zero; decide explicitly which optional layers may warn instead of fail. | Owner approved fixing with P8-O2 on 2026-06-03 | `scripts/test-all.sh` now aggregates required layer failures, writes an overall pass/fail footer, exits 1 when required layers fail, and preserves security as WARN-only; `make test-frontend` now runs frontend unit and Playwright pipelines under `bash -o pipefail` so test failures fail the target. | `bash -n scripts/test-all.sh`; `make -n test-frontend`; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1`; `git diff --check`; ReadLints clean | Unchecked; full `make test-all` and failure-injection validation were not run because they are long-running/environmental. |
| P1-S1 | RBAC middleware fails open when auth principal is absent | Critical | Fixed | 1 | `backend/libs/auth/roles.go`; `backend/libs/auth/config.go`; `docker-compose.yml` | Parent re-triage 2026-06-03 plus validation subagent: `RequireAnyRole` still called `next.ServeHTTP` when `PrincipalFromContext` was absent; command and query protected routes use this RBAC wrapper. | If a shared or internet-facing deployment starts with auth disabled, unauthenticated callers can reach routes that were intended to require FlowGo roles. | Fail closed for protected routes unless an explicit development bypass is enabled; add startup validation for secured profiles. | Owner approved fixing blocker cluster on 2026-06-03 | `RequireAnyRole` now returns 401 without a principal; auth/unit tests cover the absent-principal case; handler tests inject explicit principals instead of relying on fail-open behavior. | `go test ./backend/libs/auth -count=1`; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1`; `go test ./backend/services/workflow-query/internal/interfaces/http -count=1`; `go test ./backend/... -count=1` | Unchecked; manually verify unauthenticated protected HTTP routes fail in a running stack. |
| P1-S2 | gRPC JobWorker server has no authentication interceptor | Critical | Fixed | 1 | `backend/libs/auth/grpc.go`; `backend/services/workflow-command/cmd/server/main.go`; `backend/services/workflow-command/internal/interfaces/grpc/server.go` | Parent re-triage 2026-06-03 plus validation subagent: command server started `grpc.NewServer()` on `:50051` and registered `JobWorkerService` directly; job worker RPC handlers did not perform auth checks. | Any actor with network reachability to the port can attempt worker operations outside HTTP bearer/RBAC controls. | Add bearer-token or mTLS auth to gRPC, or make the gRPC listener opt-in/disabled for production until secured. | Owner approved fixing blocker cluster on 2026-06-03 | Added shared unary gRPC auth interceptor that verifies bearer metadata and requires FlowGo admin/client roles; command server wires the interceptor and makes `GRPC_ADDR` configurable. | `go test ./backend/libs/auth -count=1`; `go test ./backend/services/workflow-command/cmd/server -count=1`; `go test ./backend/... -count=1` | Unchecked; manually verify unauthenticated gRPC calls return `Unauthenticated` and valid admin/client tokens succeed. |
| P0-O1 | Default `make up` targets external IAM placeholders | Critical | Fixed | 0 | `Makefile`; `README.md`; `CONTRIBUTING.md`; `docker-compose.external-iam.yml` | Parent re-triage 2026-06-03 plus validation subagent: `make up`, `up-core`, `up-full`, and `up-full-cqrs` called `up-external-iam`; external-IAM compose sets placeholder auth URLs by design. | Fresh-clone startup can fail or hang on placeholder OIDC discovery instead of launching a usable local stack. | Change the default startup target to a working dev profile or make external-IAM an explicit template-only target with validation. | Owner approved fixing blocker cluster on 2026-06-03 | Default `make up`/core/full/full-cqrs plus default `logs`/`ps` now target bundled ZITADEL; docs describe external IAM as explicit configured-template usage. | `make smoke-profiles`; `make smoke-release-profiles`; `go test ./backend/... -count=1` | Unchecked; manually run `make up` from a clean checkout and sign in through bundled ZITADEL. |
| P0-O2 | Compose command and runtime services miss Kafka broker configuration | Critical | Fixed | 9 | `docker-compose.yml`; `docker-compose.external-iam.yml`; `docker-compose.zitadel.yml`; `backend/services/workflow-command/cmd/server/main.go`; `backend/services/workflow-command/cmd/runtime/main.go`; `scripts/validate_compose_kafka_wiring.sh` | Parent re-triage 2026-06-03 plus validation subagent: command and runtime binaries defaulted `KAFKA_BROKERS` to `localhost:9092`; compose files configured Kafka only for `sync-worker`, not event-publishing `app`/`workflow-runtime`. | Outbox/event publishing can target container-local Kafka instead of the compose Kafka service, breaking CQRS/event propagation. | Add broker/topic/event-bus env to command and runtime services in CQRS profiles and cover it in smoke tests. | Owner approved fixing blocker cluster on 2026-06-03 | Added `EVENT_BUS_TYPE`, `KAFKA_BROKERS=kafka:29092`, and `KAFKA_TOPIC_EVENTS` to `app` and `workflow-runtime` in all compose profiles; added service-scoped compose validator and wired it into smoke/release dry-run checks. | `docker compose -f docker-compose.yml config`; `docker compose -f docker-compose.external-iam.yml config`; `docker compose -f docker-compose.zitadel.yml config`; `make smoke-profiles`; `make smoke-release-profiles`; `RUN_NPM_CHECKS=false RUN_DOCKER_BUILDS=false make release-dry-run`; `go test ./backend/... -count=1` | Unchecked; manually confirm command/runtime containers publish to `workflow.events.v1` in a live CQRS stack. |
| P2-F1 | CQRS-backed UI lists have no freshness or syncing state after command writes | High | Fixed | 2 | `frontend/src/lib/api.ts`; `frontend/src/lib/cqrsSync.ts`; `frontend/src/lib/cqrsSync.test.ts`; `frontend/src/pages/Processes.tsx`; `frontend/src/pages/Instances.tsx`; `frontend/src/pages/Modeler.tsx`; `frontend/src/pages/Dashboard.tsx`; `docs/RUNBOOK_CQRS_SYNC.md` | Parent re-triage 2026-06-15 plus validation subagent: `getWorkflows` and `getInstances` read from `/query/...` while deploy/start/delete APIs write through command routes; Processes start/delete and Instances delete do not poll query projections after command success; Modeler deploy uses the command response but does not establish list sync; Dashboard reads query-backed instances once and labels cards `Real-time data`; the CQRS runbook covers backend parity checks but no UI freshness contract. | Users can see stale workflow/instance lists after successful writes and interpret projection lag as data loss or failed operations. | Add UI freshness handling, post-write polling, or explicit syncing state; define the command/query consistency contract for core screens and remove/qualify misleading real-time copy. | Owner approved fixing on 2026-06-15 | Added shared CQRS query-projection polling helpers and tests; Processes and Instances now show syncing notices and poll after command writes instead of silent optimism; Modeler deploys set a pending process-catalog sync hint and show catalog sync status; Dashboard now labels stats as query projection data with a last-updated time; the CQRS runbook defines the UI consistency contract. | `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build`; `git diff --check`; ReadLints clean | Unchecked; manually validate deploy-to-Processes, start-to-Instances, workflow delete, instance delete, and Dashboard refresh behavior in a live CQRS stack with projection lag. |
| P2-F2 | Processes page turns fetch failures into an empty catalog | High | Fixed | 2 | `frontend/src/pages/Processes.tsx` | Parent re-triage 2026-06-15 plus validation subagent: `fetchWorkflows` catches errors with only `console.error`, clears loading, and the render path shows `No workflows found` when the array is empty; `api.getWorkflows` already throws on non-OK responses. | Query/auth outages look like a clean environment, leading operators to create duplicates or miss incidents. | Add visible error state, retry, and distinguish empty results from failed loads; align with the existing `Instances.tsx` error/retry pattern. | Owner approved fixing with P2-F3/P2-F4 on 2026-06-15 | `Processes.tsx` now tracks workflow-list load errors, shows a retryable error state, and only shows the empty-catalog message after a successful empty fetch. | `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build`; `git diff --check`; ReadLints clean | Unchecked; manually stop or break the query workflow list path and verify `/processes` shows an error with Retry, not `No workflows found`. |
| P2-F3 | Modeler load failure leaves an editable default diagram | High | Fixed | 2 | `frontend/src/pages/Modeler.tsx` | Parent re-triage 2026-06-15 plus validation subagent: edit mode starts with the default `Process_1` canvas, `api.getWorkflow(id)` failures are only logged, missing `bpmn_xml` also leaves the default canvas in place, and deploy/save remain available. | Editing an existing workflow after a failed load can produce a wrong or empty redeploy. | Add loading/error gates for edit mode, treat missing BPMN XML as load failure, and disable deploy/save until the requested workflow loads successfully. | Owner approved fixing with P2-F2/P2-F4 on 2026-06-15 | Modeler edit mode now tracks loading/error state, treats missing BPMN XML as a load error, blocks canvas mutation plus save/deploy until the requested workflow loads, and provides Retry/Back to Processes actions. | `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build`; `git diff --check`; ReadLints clean | Unchecked; manually open `/modeler?id=<bad-or-unavailable-id>` and verify the default canvas cannot be edited, saved, or deployed until retry succeeds. |
| P2-F4 | Deploy validation details are lost behind HTTP 500 and generic UI errors | High | Fixed | 7 | `backend/services/workflow-command/internal/interfaces/http/handler.go`; `backend/services/workflow-command/internal/application/deployment.go`; `frontend/src/lib/api.ts`; `frontend/src/pages/Modeler.tsx`; `frontend/src/lib/api.test.ts` | Parent re-triage 2026-06-15 plus validation subagent: BPMN parse errors are wrapped by `deployWorkflowFromBPMN`, `deployWorkflow` maps any deployment error to 500, `api.deployWorkflow` throws only `response.statusText`, and Modeler alerts `Failed to deploy workflow`. | User-authored BPMN validation errors look like server faults and are not actionable in the UI. | Map parser/validation errors to 400 with stable details, parse API error bodies on the frontend, display the backend message to the user, and add HTTP regression coverage. | Owner approved fixing with P2-F2/P2-F3 on 2026-06-15 | BPMN parse failures are now wrapped with `ErrBPMNValidation`, HTTP deploy maps them to 400 while preserving server failures as 500, frontend deploy errors include response-body details, and Modeler shows the actual deploy error message. | `go test ./backend/services/workflow-command/internal/interfaces/http -run 'DeployBPMN' -count=1` passed after retrying with a workspace Go cache due toolchain download restrictions; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1`; `go test ./backend/services/workflow-command/internal/application -run 'Deploy|Deployment|BPMN' -count=1`; `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build`; `git diff --check`; ReadLints clean | Unchecked; manually deploy invalid BPMN through the UI/gateway and verify a 400 response plus actionable validation detail is shown to the user. |
| P3-O1 | Claimed outbox messages can stall forever in `PROCESSING` | Critical | Fixed | 3 | `backend/libs/model/engine_types.go`; `backend/services/workflow-command/internal/infrastructure/persistence/gorm_repository.go`; `backend/services/workflow-command/internal/application/engine_tx_test.go`; `backend/services/workflow-command/internal/infrastructure/persistence/gorm_repository_test.go` | Parent spot-check: `ClaimOutboxMessage` changed status from `PENDING` to `PROCESSING`; `ListPendingOutboxMessages` only selected `PENDING` rows and no lease/expiry condition was found in the claim path. | A crash after claim and before publish/mark could permanently stop specific domain events from reaching Kafka/query projections. | Add claim leases and stale `PROCESSING` reclamation, or reset expired claims on relay startup. | Owner approved fixing with P3-F1 on 2026-06-03 | Added `processing_started_at` tracking for outbox claims, included stale `PROCESSING` rows in claimable list/count after a five-minute lease, allowed stale rows to be atomically reclaimed, and cleared the processing timestamp on publish/failure/terminal failure. | `go test ./backend/services/workflow-command/internal/application -run 'Outbox|ElementInstanceCompleted|ProceedToken' -count=1`; `go test ./backend/services/workflow-command/internal/infrastructure/persistence -run 'Outbox|TerminalFailed' -count=1`; `go test ./backend/... -count=1`; `git diff --check`; ReadLints clean | Unchecked; manually verify a command-service restart after an outbox claim eventually republishes/retries the event in a live CQRS stack. |
| P3-F1 | `ElementInstanceCompleted` events use a generated process-instance key | Critical | Fixed | 3 | `backend/services/workflow-command/internal/application/navigation.go`; `backend/services/workflow-command/internal/application/engine_tx_test.go` | Parent spot-check: completion event set `ProcessInstanceKey: generateKey(instance.ID)`, and `generateKey` ignores its input and returns a new Snowflake. | Event-first consumers could link element completions to the wrong process instance and corrupt projections or audits. | Parse/use the real process instance key already stored on the instance/element instead of generating a new one. | Owner approved fixing with P3-O1 on 2026-06-03 | `ElementInstanceCompleted` now uses the parsed workflow instance ID as `ProcessInstanceKey`; regression coverage asserts the completion event key matches the real process instance key. | `go test ./backend/services/workflow-command/internal/application -run 'Outbox|ElementInstanceCompleted|ProceedToken' -count=1`; `go test ./backend/... -count=1`; `git diff --check`; ReadLints clean | Unchecked; manually verify consumed `ElementInstanceCompleted` events share the same process-instance aggregate key as the instance lifecycle events. |
| P3-S1 | Partial variable updates re-publish the full instance context | High | Fixed | 5 | `backend/services/workflow-command/internal/application/jobs.go`; `backend/services/workflow-command/internal/application/engine.go`; `backend/services/workflow-command/internal/application/events.go`; `backend/services/workflow-command/internal/application/step_executors.go`; `backend/services/workflow-command/internal/application/engine_tx_test.go`; `backend/services/workflow-command/internal/application/engine_variables_test.go` | Parent spot-check: `CompleteJob` merged worker variables into `instance.Context` then called `persistVariables` with the full context; `persistVariables` loops every key and publishes `VariableUpdated` with the serialized value. | Small worker updates could fan out all variables to outbox/Kafka/query/log surfaces, increasing load and sensitive-data exposure. | Persist and publish only changed keys; reserve full-context persistence for explicit replacement operations. | Owner approved fixing on 2026-06-03 | `CompleteJob` now persists only worker-provided variable deltas; signal/message correlation persists only payload deltas; service/script executors persist only changed context keys; redundant post-navigation full-context persistence was removed from completion paths; `persistVariables` now documents its delta-only contract. Regression coverage asserts a job completion with existing variables emits only the updated variable and helper tests cover variable snapshot/diff behavior. | `go test ./backend/services/workflow-command/internal/application -run 'Changed|Variable|CompleteJob|Outbox|ElementInstanceCompleted|ProceedToken' -count=1`; `go test ./backend/... -count=1`; `git diff --check`; ReadLints clean | Unchecked; manually verify a worker completion with one variable update emits only that variable on Kafka/outbox and leaves existing context intact in projections. |

## Decision Log

| Date | Finding/Cluster | Decision | Rationale | Owner |
| --- | --- | --- | --- | --- |
| 2026-06-03 | Audit setup | Opened thermo-nuclear audit ledger and session prompt | Owner requested durable audit/fix workflow across the repository | Owner |
| 2026-06-03 | First audit fan-out | Approved and launched five read-only audit subagents for phases 0/9, 1/4, 2/7, 3/5, and 8/10 | Owner approved the recommended broad audit fan-out before any product code changes | Owner |
| 2026-06-03 | Phase 8/10 audit report | Recorded three parent-verified Open findings from the quality/tests audit; held remaining candidates for cross-agent dedupe | Top claims were spot-checked in live code; no fixes approved yet | Agent |
| 2026-06-03 | First audit fan-out complete | Recorded eleven additional parent-verified Open findings across release boot, auth/API, workflow UX, and persistence/CQRS; held lower-priority or policy-dependent candidates for later validation | All five subagents completed; critical claims were deduped and spot-checked before ledger entry | Agent |
| 2026-06-03 | Triage P1-S1/P1-S2/P0-O1/P0-O2 | Revalidated the recommended security/release blocker cluster and kept all four findings Open | Parent spot-check confirmed live code/config evidence; owner has not approved product code changes yet | Agent |
| 2026-06-03 | Validation subagents for blocker cluster | Read-only subagents corroborated P1-S1, P1-S2, P0-O1, and P0-O2 with deployment nuance | P1-S2 is mostly internal-network exposed rather than published externally; P0-O2 is compose-specific because Helm already wires Kafka env; no product changes approved | Agent |
| 2026-06-03 | Owner approval for blocker cluster | Approved fixing P1-S1, P1-S2, P0-O1, and P0-O2 | Owner explicitly requested `fix P1-S1, P1-S2, P0-O1, P0-O2` | Owner |
| 2026-06-03 | Fixed P1-S1/P1-S2/P0-O1/P0-O2 | Implemented focused auth, gRPC, Makefile, compose, docs, and test updates for the approved blocker cluster | Backend suite and compose smoke checks passed; live-stack manual QA remains unchecked | Agent |
| 2026-06-03 | Strengthened P0-O2 smoke coverage | Added service-scoped compose Kafka wiring validation to smoke profiles and release dry run | Compose planning subagent noted that raw `docker compose config` could pass because `sync-worker` had Kafka env even when `app`/`workflow-runtime` did not | Agent |
| 2026-06-03 | Triage P3-O1/P3-F1/P3-S1 | Revalidated all three persistence/event correctness findings and kept them Open with a fix recommendation | Read-only validation subagents corroborated each finding; parent spot-check confirmed outbox `PROCESSING` rows have no reclaim path, `ElementInstanceCompleted` uses `generateKey(instance.ID)`, and `CompleteJob` persists the full merged context for partial variable updates | Agent |
| 2026-06-03 | Owner approval for P3-O1/P3-F1 | Approved fixing P3-O1 and P3-F1 as a focused persistence/event correctness cluster | Owner explicitly requested `approve P3-O1 + P3-F1`; P3-S1 remains Open and unapproved for implementation | Owner |
| 2026-06-03 | Fixed P3-O1/P3-F1 | Implemented stale outbox claim reclamation and corrected element-completion process-instance keys | Focused application/persistence tests and full backend suite passed; live CQRS restart/event-consumption QA remains unchecked | Agent |
| 2026-06-03 | Owner approval for P3-S1 | Approved fixing P3-S1 | Owner explicitly requested `fix P3-S1` | Owner |
| 2026-06-03 | Fixed P3-S1 | Implemented delta-only variable persistence for worker completions, signal/message payloads, and in-process context mutations | Focused application regressions/helper tests and full backend suite passed; live Kafka/outbox projection QA remains unchecked | Agent |
| 2026-06-03 | Triage P8-Q1/P8-O2 | Revalidated both release/operational findings and kept them Open with a fix recommendation | Parent spot-check confirmed `scripts/test-all.sh` captures per-layer failures but does not aggregate them into a final non-zero exit, and `deployWorkflow` still reads the BPMN request body with unbounded `io.ReadAll`; read-only validation subagents were launched for corroboration | Agent |
| 2026-06-03 | Owner approval for P8-Q1/P8-O2 | Approved fixing the release/operational correctness cluster | Owner explicitly requested `fix cluster: P8-Q1 + P8-O2` | Owner |
| 2026-06-03 | Fixed P8-Q1/P8-O2 | Implemented required-layer failure aggregation for the full test orchestrator, hardened `make test-frontend` failure propagation, and capped BPMN deploy request bodies at 20 MiB with 413 handling | Focused HTTP handler tests, shell/Make dry-run validation, diff whitespace check, and lints passed; full `make test-all` and live deploy-size QA remain unchecked | Agent |
| 2026-06-15 | Triage P2-F2/P2-F3/P2-F4 | Revalidated the UI correctness cluster and kept all three findings Open with a fix recommendation | Read-only validation subagents corroborated each finding; parent spot-check confirmed Processes conflates failed workflow fetches with empty results, Modeler edit-mode load failures leave deploy/save enabled on the default canvas, and deploy validation errors are still returned as 500 then hidden behind generic frontend errors | Agent |
| 2026-06-15 | Owner approval for P2-F2/P2-F3/P2-F4 | Approved fixing the UI correctness cluster | Owner explicitly requested `fix cluster: P2-F2 P2-F3 P2-F4` | Owner |
| 2026-06-15 | Fixed P2-F2/P2-F3/P2-F4 | Implemented focused workflow-list error handling, Modeler edit-mode load gates, BPMN validation 400 mapping, and deploy error detail propagation | Focused backend HTTP tests, frontend lint/test/build, diff whitespace check, and lints passed; live browser/gateway QA remains unchecked | Agent |
| 2026-06-15 | Triage P2-F1 | Revalidated the CQRS UI freshness finding and kept it Open with a fix recommendation | Read-only validation subagent corroborated the finding; parent spot-check confirmed query-backed list reads remain eventually consistent after command writes with no post-write polling/syncing UX, while Dashboard still labels query-backed data as real-time | Agent |
| 2026-06-15 | Owner approval for P2-F1 | Approved fixing the CQRS UI freshness finding | Owner explicitly requested `fix P2-F1` | Owner |
| 2026-06-15 | Fixed P2-F1 | Implemented frontend CQRS sync helpers, post-write list syncing UX, honest Dashboard freshness copy, and a documented UI consistency contract | Frontend lint/test/build, diff whitespace check, and lints passed; live CQRS projection-lag QA remains unchecked | Agent |
| 2026-06-15 | Triage P8-O1 | Revalidated the signal publishing scalability finding and kept it Open with a fix recommendation | Two read-only validation subagents corroborated the finding; parent spot-check confirmed global active-element scans in both signal publish paths and BPMN XML parsing through `getWorkflowDefinition` for each candidate match attempt | Agent |
| 2026-06-15 | Owner approval for P8-O1 | Approved fixing the signal publishing scalability finding | Owner explicitly requested `fix triage P8-O1` | Owner |
| 2026-06-15 | Fixed P8-O1 | Implemented a scoped signal-candidate query, per-publish workflow definition memoization, and pre-transaction signal match filtering | Focused application/persistence tests, full workflow-command tests, diff whitespace check, and lints passed; live signal-load QA remains unchecked | Agent |

## Release QA Checklist

Add a row for every user-visible fix. Tests alone are not sufficient to claim release readiness when manual QA remains unchecked.

| Area | Scenario | Related finding | Automated checks | Manual QA status | Notes |
| --- | --- | --- | --- | --- | --- |
| General | Start selected deployment profile and load frontend | P0-O1, P0-O2 | `make smoke-profiles`; `make smoke-release-profiles`; `RUN_NPM_CHECKS=false RUN_DOCKER_BUILDS=false make release-dry-run`; `go test ./backend/... -count=1` | Unchecked | Baseline release smoke item; default `make up` now targets bundled ZITADEL and smoke checks assert command/runtime Kafka env. |
| Workflow | Deploy BPMN, start instance, observe instance details | TBD | TBD | Unchecked | Baseline release smoke item. |
| Persistence/CQRS | Restart command service after an outbox message is claimed but before publish/mark; verify stale message is retried and projected | P3-O1 | `go test ./backend/services/workflow-command/internal/application -run 'Outbox|ElementInstanceCompleted|ProceedToken' -count=1`; `go test ./backend/services/workflow-command/internal/infrastructure/persistence -run 'Outbox|TerminalFailed' -count=1`; `go test ./backend/... -count=1` | Unchecked | Code now reclaims `PROCESSING` outbox rows after the claim lease; live restart behavior still needs manual QA. |
| Event correctness | Complete a terminal workflow element and verify `ElementInstanceCompleted.process_instance_key` matches the process instance key / Kafka aggregate | P3-F1 | `go test ./backend/services/workflow-command/internal/application -run 'Outbox|ElementInstanceCompleted|ProceedToken' -count=1`; `go test ./backend/... -count=1` | Unchecked | Unit coverage asserts the emitted protobuf uses the parsed instance key. |
| Event privacy/load | Complete a worker job with one updated variable while the instance has multiple existing variables; verify only the updated variable is emitted and projected | P3-S1 | `go test ./backend/services/workflow-command/internal/application -run 'Changed|Variable|CompleteJob|Outbox|ElementInstanceCompleted|ProceedToken' -count=1`; `go test ./backend/... -count=1` | Unchecked | Unit coverage asserts only the worker delta emits `VariableUpdated` and helper coverage verifies context diff behavior; live Kafka/outbox projection behavior still needs manual QA. |
| Auth/IAM | Sign in as admin/viewer/client-equivalent role and verify access boundaries | P1-S1, P1-S2, P0-O1 | `go test ./backend/libs/auth -count=1`; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1`; `go test ./backend/services/workflow-query/internal/interfaces/http -count=1`; `go test ./backend/... -count=1` | Unchecked | Verify protected HTTP routes and gRPC worker RPCs reject unauthenticated callers. |
| Release QA | Inject failures into required `make test-all` layers and verify the command exits non-zero while security remains WARN-only | P8-Q1 | `bash -n scripts/test-all.sh`; `git diff --check`; ReadLints clean | Unchecked | Full orchestrator/failure-injection QA is intentionally separate from the focused code checks because it requires the composed stack and longer-running suites. |
| Deploy limits | POST BPMN under and over the upload limit through direct command-service and gateway paths | P8-O2 | `go test ./backend/services/workflow-command/internal/interfaces/http -run 'DeployBPMN' -count=1`; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1` | Unchecked | Handler coverage asserts oversized uploads return 413 and do not create a workflow; live path should verify client/gateway behavior. |
| Processes UI | Break or stop the query workflow-list endpoint and reload `/processes` | P2-F2 | `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build` | Unchecked | Verify the page shows a load error with Retry and does not show `No workflows found` for failed fetches. |
| Modeler edit load | Open `/modeler?id=<bad-or-unavailable-id>` and then restore the API and retry | P2-F3 | `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build` | Unchecked | Verify edit mode blocks canvas mutation, Save XML, and Deploy Process until the workflow loads successfully. |
| Deploy validation | Deploy invalid BPMN through the UI and gateway/direct command-service path | P2-F4 | `go test ./backend/services/workflow-command/internal/interfaces/http -run 'DeployBPMN' -count=1`; `go test ./backend/services/workflow-command/internal/interfaces/http -count=1`; `npm --prefix frontend test`; `npm --prefix frontend run build` | Unchecked | Verify invalid BPMN returns 400 and the UI shows the validation detail from the backend response body. |
| CQRS UI freshness | Deploy workflow, start instance, delete workflow, delete instance, and refresh Dashboard in a live CQRS stack | P2-F1 | `npm --prefix frontend run lint`; `npm --prefix frontend test`; `npm --prefix frontend run build`; `git diff --check`; ReadLints clean | Unchecked | Verify syncing notices appear after command writes, query-backed lists settle once projections catch up, timeout copy does not imply command failure, and Dashboard no longer claims real-time data. |
| Signal runtime scalability | Publish a signal while many active service/user task elements and a small number of BPMN signal catches exist | P8-O1 | `go test ./backend/services/workflow-command/internal/application -run 'PublishSignal|Signal|WorkflowDefinition|CompleteJob' -count=1`; `go test ./backend/services/workflow-command/internal/infrastructure/persistence -run 'Element|Signal|Process' -count=1`; `go test ./backend/services/workflow-command/... -count=1`; `git diff --check`; ReadLints clean | Unchecked | Verify signal publish queries only signal-capable active elements, matching signal catches still correlate, and latency does not grow with unrelated active service/user tasks. |
| SDK | Run SDK smoke against local stack with client token | TBD | TBD | Unchecked | Baseline release smoke item. |

## Operating Rules

- No product code changes until the owner approves a specific finding ID or related cluster.
- Verify each finding live in code before fixing.
- Work one finding or tightly related cluster at a time.
- Update this ledger after every finding, decision, fix, test run, or manual QA result.
- Keep fixes focused; do not start broad refactors.
- Preserve owner decisions. Do not reopen deferred or closed findings without explicit approval.

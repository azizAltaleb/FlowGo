services:
  app:
    image: ${ARTIFICIALFLOW_IMAGE_REGISTRY:-artificialflow}/workflow-command:${ARTIFICIALFLOW_IMAGE_TAG:-1.0.0}
    build: !reset null

  workflow-runtime:
    image: ${ARTIFICIALFLOW_IMAGE_REGISTRY:-artificialflow}/workflow-runtime:${ARTIFICIALFLOW_IMAGE_TAG:-1.0.0}
    build: !reset null

  workflow-query:
    image: ${ARTIFICIALFLOW_IMAGE_REGISTRY:-artificialflow}/workflow-query:${ARTIFICIALFLOW_IMAGE_TAG:-1.0.0}
    build: !reset null

  sync-worker:
    image: ${ARTIFICIALFLOW_IMAGE_REGISTRY:-artificialflow}/sync-worker:${ARTIFICIALFLOW_IMAGE_TAG:-1.0.0}
    build: !reset null

  frontend:
    image: ${ARTIFICIALFLOW_IMAGE_REGISTRY:-artificialflow}/frontend:${ARTIFICIALFLOW_IMAGE_TAG:-1.0.0}
    build: !reset null
